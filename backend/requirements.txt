# Production runtime dependencies.
#
# INSTALL (two steps — see requirements-2ddoc.txt for why):
#   pip install -r requirements.txt
#   pip install --no-deps -r requirements-2ddoc.txt
#
# Dev/CI additionally: pip install -r requirements-dev.txt

fastapi>=0.109.0
uvicorn[standard]>=0.47.0
sqlalchemy>=2.0.25
asyncpg>=0.30.0
psycopg2-binary==2.9.10
alembic==1.13.1
pydantic>=2.10.0
pydantic-settings>=2.10.0
# JWT: PyJWT, not python-jose. python-jose unconditionally pulls `ecdsa`,
# which carries PYSEC-2026-1325 with no fixed release upstream. We sign
# HS256 only (settings.ALGORITHM), so PyJWT covers it and needs no
# elliptic-curve backend at all.
pyjwt>=2.10.0
passlib==1.7.4
argon2-cffi==23.1.0
python-multipart>=0.0.20
google-genai>=0.2.0
httpx>=0.28.0
google-auth==2.28.1
resend>=0.8.0
python-dotenv>=1.1.0
greenlet>=3.1.0
email-validator==2.3.0
slowapi==0.1.9
# Imported directly by app/utils/encryption.py (PII encryption) and
# app/services/credential.py (Ed25519 credential signing), so declared
# explicitly rather than inherited transitively. Floor is the fix for
# PYSEC-2026-35 / -1284 / -2141 / GHSA-537c-gmf6-5ccf.
cryptography>=48.0.1
# Netflix-style infrastructure
redis==5.0.1
celery==5.3.4
tenacity==8.2.3
# Cloud storage (Cloudflare R2 / S3)
boto3==1.34.0
aiofiles==23.2.1
python-dateutil>=2.8.0
# Monitoring
sentry-sdk[fastapi]>=2.7.0
reportlab==4.1.0
Jinja2>=3.1.2
pillow>=10.2.0
pytesseract>=0.3.13
pylibdmtx>=0.1.10
PyMuPDF>=1.24.0
setuptools>=65.0.0
google-antigravity>=0.1.0
